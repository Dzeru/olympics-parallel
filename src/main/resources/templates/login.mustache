<!DOCTYPE html>
<html xmlns:=http://www.w3.org/1999/xhtml>
<head>
    {{> meta}}
    <title>Olympics Parallel Login</title>
</head>
<body>
<section class="container p-5">
    {{#error}}<div class="p-3">
        <span class="alert alert-danger">{{error}}</span>
    </div>{{/error}}
        <form name="loginForm" action="/login" method="post" accept-charset="utf-8">
            <div class="form-group">
                <label for="email">Username</label>
                <input class="form-control" id="username" type="text" name="username" placeholder="raven">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input class="form-control" id="password" type="password" name="password" placeholder="fiverings">
            </div>
            <input class="btn btn-info mt-1 mb-3" type="submit" value="Login">
        </form>
</section>
</body>
</html>
